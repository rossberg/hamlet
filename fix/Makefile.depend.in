# System-specific library work-arounds
Char-$(SYSTEM).$(EXT) :
CharVector-$(SYSTEM).$(EXT) :
CommandLine-$(SYSTEM).$(EXT) :
Large-$(SYSTEM).$(EXT) :
ListPair-$(SYSTEM).$(EXT) :
OS-$(SYSTEM).$(EXT) :
String-$(SYSTEM).$(EXT) : Char-$(SYSTEM).$(EXT)
Real-$(SYSTEM).$(EXT) :
TextIO-$(SYSTEM).$(EXT) :
Word-$(SYSTEM).$(EXT) :
